[project]
name = "daily-git-commit"
version = "1.0.0"
description = "Automatically commit to GitHub once per day"
requires-python = ">=3.10"

[project.scripts]
daily-commit = "daily_commit.cli:main"

[tool.daily_commit]
ci-python-version = "3.14"